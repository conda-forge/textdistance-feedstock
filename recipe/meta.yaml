{% set name = "textdistance" %}
{% set version = "3.1.0" %}
{% set sha256 = "415eebe1a41fb1048d9978c9abe964fa839ca9b97007f0a8931cdf360c686ed7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
  run:
    - python

test:
  imports:
    - textdistance

about:
  home: https://github.com/orsinium/textdistance
  license: LGPL-3.0
  license_family: LGPL
  license_file: LICENSE
  summary: 'TextDistance – python library for comparing distance between two or more sequences by many algorithms.'

  description: |
    TextDistance – python library for comparing distance between two or more sequences by many algorithms.
  doc_url: https://pypi.org/project/textdistance/#description
  dev_url: https://github.com/orsinium/textdistance

extra:
  recipe-maintainers:
    - cu8blank
